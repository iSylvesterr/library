-- Decompiled with Potassium's decompiler.

local u1 = 300;
local u2 = 10;
local u3 = 5;
local u4 = 40;
local u5 = 630;
local u6 = 120;
local u7 = 5;
local u8 = 120;
local u9 = 120;
local u10 = 1000;
local u11 = nil;

local function AcquireRunnerThreadAndCallEventHandler(p12, ...) -- Line: 199
    -- upvalues: u11 (ref)
    local v13 = u11;
    u11 = nil;
    p12(...);
    u11 = v13;
end;

local function RunEventHandlerInFreeThread(...) -- Line: 207
    -- upvalues: AcquireRunnerThreadAndCallEventHandler (copy)
    AcquireRunnerThreadAndCallEventHandler(...);

    while true do
        AcquireRunnerThreadAndCallEventHandler(coroutine.yield());
    end;
end;

local u14 = {};
u14.__index = u14;
local u15 = {};
u15.__index = u15;

function u14.Disconnect(p16) -- Line: 220
    if p16.is_connected == false then
        return;
    end;

    local signal = p16.signal;
    p16.is_connected = false;
    signal.listener_count = signal.listener_count - 1;

    if signal.head == p16 then
        signal.head = p16.next;

        return;
    end;

    local head = signal.head;

    while head ~= nil and head.next ~= p16 do
        head = head.next;
    end;

    if head ~= nil then
        head.next = p16.next;
    end;
end;

function u15.New() -- Line: 242
    -- upvalues: u15 (copy)
    local v17 = {
        head = nil,
        listener_count = 0
    };
    setmetatable(v17, u15);

    return v17;
end;

function u15.Connect(p18, p19) -- Line: 252
    -- upvalues: u14 (copy)
    if type(p19) ~= "function" then
        error((`[{script.Name}]: "listener" must be a function; Received {typeof(p19)}`));
    end;

    local v20 = {
        is_connected = true,
        listener = p19,
        signal = p18,
        next = p18.head
    };
    setmetatable(v20, u14);
    p18.head = v20;
    p18.listener_count = p18.listener_count + 1;

    return v20;
end;

function u15.GetListenerCount(p21) -- Line: 271
    return p21.listener_count;
end;

function u15.Fire(p22, ...) -- Line: 275
    -- upvalues: u11 (ref), RunEventHandlerInFreeThread (copy)
    local head = p22.head;

    while head ~= nil do
        if head.is_connected == true then
            if not u11 then
                u11 = coroutine.create(RunEventHandlerInFreeThread);
            end;

            task.spawn(u11, head.listener, ...);
        end;

        head = head.next;
    end;
end;

function u15.Wait(p23) -- Line: 288
    local u24 = coroutine.running();
    local u25 = nil;
    u25 = p23:Connect(function(...) -- Line: 291
        -- upvalues: u25 (ref), u24 (copy)
        u25:Disconnect();
        task.spawn(u24, ...);
    end);

    return coroutine.yield();
end;

local u26 = table.freeze({
    New = u15.New
});
local u27 = {};
local u28 = {};
local u29 = {};
local DataStoreService = game:GetService("DataStoreService");
local MessagingService = game:GetService("MessagingService");
local HttpService = game:GetService("HttpService");
local RunService = game:GetService("RunService");
local PlaceId = game.PlaceId;
local JobId = game.JobId;
local u30 = 1;
local u31 = os.clock();
local u32 = 0;
local u33 = 0;
local u34 = 0;
local u35 = 0;
local u36 = RunService:IsStudio();
local u37 = "NotReady";
local u38 = {};
local u39 = {};
local u40 = false;
local u41 = u26.New();
local u42 = u26.New();
local u43 = {};

local function WaitInUpdateQueue(u44) -- Line: 347
    -- upvalues: u43 (copy)
    local v45;

    if u43[u44] == nil then
        u43[u44] = {};
        v45 = true;
    else
        v45 = false;
    end;

    local u46 = u43[u44];

    if v45 == false then
        table.insert(u46, coroutine.running());
        coroutine.yield();
    end;

    return function() -- Line: 362
        -- upvalues: u46 (copy), u43 (ref), u44 (copy)
        local v47 = table.remove(u46, 1);

        if v47 == nil then
            u43[u44] = nil;

            return;
        end;

        coroutine.resume(v47);
    end;
end;

local function SessionToken(p48, p49, p50) -- Line: 372
    -- upvalues: u37 (ref)
    return (p50 == true and "U_" or (u37 ~= "Access" and "M_" or "L_")) .. p48 .. "\0" .. p49;
end;

local function DeepCopyTable(p51) -- Line: 386
    -- upvalues: DeepCopyTable (copy)
    local v52 = {};

    for i, v in pairs(p51) do
        if type(v) == "table" then
            v52[i] = DeepCopyTable(v);
        else
            v52[i] = v;
        end;
    end;

    return v52;
end;

local function ReconcileTable(p53, p54) -- Line: 398
    -- upvalues: DeepCopyTable (copy), ReconcileTable (copy)
    for i, v in pairs(p54) do
        if type(i) == "string" then
            if p53[i] == nil then
                if type(v) == "table" then
                    p53[i] = DeepCopyTable(v);
                else
                    p53[i] = v;
                end;
            elseif type(p53[i]) == "table" and type(v) == "table" then
                ReconcileTable(p53[i], v);
            end;
        end;
    end;
end;

local function RegisterError(p55, p56, p57) -- Line: 414
    -- upvalues: u29 (copy), u41 (copy)
    warn((`[{script.Name}]: DataStore API error (STORE:{p56}; KEY:{p57}) - {tostring(p55)}`));
    table.insert(u29, os.clock());
    u41:Fire(tostring(p55), p56, p57);
end;

local function RegisterOverwrite(p58, p59) -- Line: 420
    -- upvalues: u42 (copy)
    warn((`[{script.Name}]: Invalid profile was overwritten (STORE:{p58}; KEY:{p59})`));
    u42:Fire(p58, p59);
end;

local function NewMockDataStoreKeyInfo(p60) -- Line: 425
    -- upvalues: DeepCopyTable (copy)
    local v61 = tostring(p60.VersionId or 0);
    local u62 = p60.MetaData or {};
    local u63 = p60.UserIds or {};

    return {
        CreatedTime = p60.CreatedTime,
        UpdatedTime = p60.UpdatedTime,
        Version = string.rep("0", 16) .. "." .. string.rep("0", 10 - string.len(v61)) .. v61 .. "." .. string.rep("0", 16) .. ".01",

        GetMetadata = function() -- Line: 442, Name: GetMetadata
            -- upvalues: DeepCopyTable (ref), u62 (copy)
            return DeepCopyTable(u62);
        end,

        GetUserIds = function() -- Line: 446, Name: GetUserIds
            -- upvalues: DeepCopyTable (ref), u63 (copy)
            return DeepCopyTable(u63);
        end
    };
end;

local function MockUpdateAsync(p64, p65, p66, p67, p68) -- Line: 452
    -- upvalues: NewMockDataStoreKeyInfo (copy), DeepCopyTable (copy)
    local v69 = p64[p65];

    if v69 == nil then
        v69 = {};
        p64[p65] = v69;
    end;

    local v70 = os.time() * 1000;
    local v71 = math.floor(v70);
    local v72 = v69[p66];
    local v73;

    if v72 == nil then
        v73 = true;

        if p68 ~= true then
            v72 = {
                Data = nil,
                VersionId = 0,
                CreatedTime = v71,
                UpdatedTime = v71,
                UserIds = {},
                MetaData = {}
            };
            v69[p66] = v72;
        end;
    else
        v73 = false;
    end;

    local v74;

    if v73 == false then
        v74 = NewMockDataStoreKeyInfo(v72) or nil;
    else
        v74 = nil;
    end;

    local v75;

    if v72 then
        v75 = v72.Data;
    else
        v75 = v72;
    end;

    local v76, v77, v78 = p67(v75, v74);

    if v76 == nil then
        return nil;
    end;

    if v72 ~= nil and p68 ~= true then
        v72.Data = DeepCopyTable(v76);
        v72.UserIds = DeepCopyTable(v77 or {});
        v72.MetaData = DeepCopyTable(v78 or {});
        v72.VersionId = v72.VersionId + 1;
        v72.UpdatedTime = v71;
    end;

    local v79 = DeepCopyTable(v76);
    local v80;

    if v72 == nil then
        v80 = nil;
    else
        v80 = NewMockDataStoreKeyInfo(v72) or nil;
    end;

    return v79, v80;
end;

local function UpdateAsync(u81, u82, u83, u84, u85, u86) -- Line: 498
    -- upvalues: WaitInUpdateQueue (copy), u37 (ref), MockUpdateAsync (copy), u39 (copy), u38 (copy), u42 (copy), u29 (copy), u41 (copy)
    local u87 = nil;
    local u88 = nil;
    local v89 = WaitInUpdateQueue((u84 == true and "U_" or (u37 ~= "Access" and "M_" or "L_")) .. u81.Name .. "\0" .. u82);
    local success, result = pcall(function() -- Line: 511
        -- upvalues: u83 (copy), u84 (copy), u87 (ref), u88 (ref), MockUpdateAsync (ref), u39 (ref), u81 (copy), u82 (copy), u85 (copy), u37 (ref), u38 (ref), u86 (copy)
        local function v94(p90) -- Line: 512
            -- upvalues: u83 (ref)
            local v91 = false;
            local v92 = false;
            local v93 = { 0, {} };

            if p90 == nil then
                v91 = true;
            elseif type(p90) == "table" then
                if type(p90.Data) == "table" and (type(p90.MetaData) == "table" and type(p90.GlobalUpdates) == "table") then
                    p90.WasOverwritten = false;
                    v93 = p90.GlobalUpdates;

                    if u83.ExistingProfileHandle ~= nil then
                        u83.ExistingProfileHandle(p90);
                    end;
                elseif p90.Data == nil and (p90.MetaData == nil and type(p90.GlobalUpdates) == "table") then
                    p90.WasOverwritten = false;
                    v93 = p90.GlobalUpdates or v93;
                    v91 = true;
                else
                    v91 = true;
                    v92 = true;
                end;
            else
                v91 = true;
                v92 = true;
            end;

            if v91 == true then
                p90 = {
                    GlobalUpdates = v93
                };

                if u83.MissingProfileHandle ~= nil then
                    u83.MissingProfileHandle(p90);
                end;
            end;

            if u83.EditProfile ~= nil then
                u83.EditProfile(p90);
            end;

            if v92 == true then
                p90.WasOverwritten = true;
            end;

            return p90, p90.UserIds, p90.RobloxMetaData;
        end;

        if u84 == true then
            local v95, v96 = MockUpdateAsync(u39, u81.Name, u82, v94, u85);
            u87 = v95;
            u88 = v96;
            task.wait();

            return;
        end;

        if u37 == "Access" then
            if u85 == true then
                if u86 == nil then
                    local v97, v98 = u81.data_store:GetAsync(u82);
                    u87 = v97;
                    u88 = v98;
                else
                    local success, result = pcall(function() -- Line: 588
                        -- upvalues: u87 (ref), u88 (ref), u81 (ref), u82 (ref), u86 (ref)
                        local v99, v100 = u81.data_store:GetVersionAsync(u82, u86);
                        u87 = v99;
                        u88 = v100;
                    end);

                    if success == false and (type(result) == "string" and string.find(result, "not valid") ~= nil) then
                        warn(`[{script.Name}]: Passed version argument is not valid; Traceback:\n` .. debug.traceback());
                    end;
                end;

                u87 = v94(u87);

                return;
            end;

            local v101, v102 = u81.data_store:UpdateAsync(u82, v94);
            u87 = v101;
            u88 = v102;

            return;
        end;

        local v103, v104 = MockUpdateAsync(u38, u81.Name, u82, v94, u85);
        u87 = v103;
        u88 = v104;
        task.wait();
    end);
    v89();

    if success == true and type(u87) == "table" then
        if u87.WasOverwritten == true and u85 ~= true then
            local Name = u81.Name;
            warn((`[{script.Name}]: Invalid profile was overwritten (STORE:{Name}; KEY:{u82})`));
            u42:Fire(Name, u82);
        end;

        return u87, u88;
    end;

    local v105 = result or "Undefined error";
    local Name = u81.Name;
    warn((`[{script.Name}]: DataStore API error (STORE:{Name}; KEY:{u82}) - {tostring(v105)}`));
    table.insert(u29, os.clock());
    u41:Fire(tostring(v105), Name, u82);

    return nil;
end;

local function IsThisSession(p106) -- Line: 627
    -- upvalues: PlaceId (copy), JobId (copy)
    local v107;

    if p106[1] == PlaceId then
        v107 = p106[2] == JobId;
    else
        v107 = false;
    end;

    return v107;
end;

local function ReadMockFlag() -- Line: 631
    -- upvalues: u40 (ref)
    local v108 = u40;
    u40 = false;

    return v108;
end;

local function WaitForStoreReady(p109) -- Line: 637
    while p109.is_ready == false do
        task.wait();
    end;
end;

local function AddProfileToAutoSave(p110) -- Line: 643
    -- upvalues: u27 (copy), u28 (copy), u30 (ref), u31 (ref)
    u27[p110.session_token] = p110;
    table.insert(u28, u30, p110);

    if #u28 > 1 then
        u30 = u30 + 1;

        return;
    end;

    if #u28 == 1 then
        u31 = os.clock();
    end;
end;

local function RemoveProfileFromAutoSave(p111) -- Line: 658
    -- upvalues: u27 (copy), u28 (copy), u30 (ref)
    u27[p111.session_token] = nil;
    local v112 = table.find(u28, p111);

    if v112 ~= nil then
        table.remove(u28, v112);

        if v112 < u30 then
            u30 = u30 - 1;
        end;

        if u28[u30] == nil then
            u30 = 1;
        end;
    end;
end;

local function SaveProfileAsync(u113, u114, u115, p116) -- Line: 674
    -- upvalues: u27 (copy), u28 (copy), u30 (ref), u34 (ref), UpdateAsync (copy), PlaceId (copy), JobId (copy), SaveProfileAsync (copy), DeepCopyTable (copy)
    if type(u113.Data) ~= "table" then
        error((`[{script.Name}]: Developer code likely set "Profile.Data" to a non-table value! (STORE:{u113.ProfileStore.Name}; KEY:{u113.Key})`));
    end;

    u113.OnSave:Fire();

    if u114 == true then
        u113.OnLastSave:Fire(p116 or "Manual");
    end;

    if u114 == true and u115 ~= true then
        if u113.roblox_message_subscription ~= nil then
            u113.roblox_message_subscription:Disconnect();
        end;

        u27[u113.session_token] = nil;
        local v117 = table.find(u28, u113);

        if v117 ~= nil then
            table.remove(u28, v117);

            if v117 < u30 then
                u30 = u30 - 1;
            end;

            if u28[u30] == nil then
                u30 = 1;
            end;
        end;

        u113.OnSessionEnd:Fire();
    end;

    u34 = u34 + 1;
    local v118 = true;
    local v119 = 1;

    while v118 == true do
        if u114 ~= true then
            v118 = false;
        end;

        local v124, v125 = UpdateAsync(u113.ProfileStore, u113.Key, {
            ExistingProfileHandle = nil,
            MissingProfileHandle = nil,

            EditProfile = function(p120) -- Line: 709, Name: EditProfile
                -- upvalues: u115 (copy), PlaceId (ref), JobId (ref), u113 (copy), u114 (copy)
                local v121 = false;

                if u115 == true then
                    v121 = true;
                else
                    local ActiveSession = p120.MetaData.ActiveSession;
                    local SessionLoadCount = p120.MetaData.SessionLoadCount;

                    if type(ActiveSession) == "table" then
                        if ActiveSession[1] == PlaceId then
                            v121 = ActiveSession[2] == JobId;
                        else
                            v121 = false;
                        end;

                        if v121 then
                            v121 = SessionLoadCount == u113.load_index;
                        end;
                    end;
                end;

                if v121 == true then
                    local locked_global_updates = u113.locked_global_updates;
                    local v122 = p120.GlobalUpdates[2];

                    if next(locked_global_updates) ~= nil then
                        local v123 = 1;

                        while v123 <= #v122 do
                            if locked_global_updates[v122[v123][1]] == true then
                                table.remove(v122, v123);
                            else
                                v123 = v123 + 1;
                            end;
                        end;
                    end;

                    p120.Data = u113.Data;
                    p120.RobloxMetaData = u113.RobloxMetaData;
                    p120.UserIds = u113.UserIds;

                    if u115 == true then
                        p120.MetaData.ActiveSession = nil;
                        p120.MetaData.ForceLoadSession = nil;
                    else
                        p120.MetaData.LastUpdate = os.time();

                        if u114 == true then
                            p120.MetaData.ActiveSession = nil;
                        end;
                    end;
                end;
            end
        }, u113.is_mock);

        if v124 == nil or v125 == nil then
            if v118 == true then
                task.wait(v119);
                v119 = math.min(p116 == "Shutdown" and 8 or 20, v119 * 2);
            end;
        else
            if u115 == true then
                break;
            end;

            v118 = false;
            local ActiveSession = v124.MetaData.ActiveSession;
            local SessionLoadCount = v124.MetaData.SessionLoadCount;
            local v126;

            if type(ActiveSession) == "table" then
                if ActiveSession[1] == PlaceId then
                    v126 = ActiveSession[2] == JobId;
                else
                    v126 = false;
                end;

                if v126 then
                    v126 = SessionLoadCount == u113.load_index;
                end;
            else
                v126 = false;
            end;

            local ForceLoadSession = v124.MetaData.ForceLoadSession;
            local v127;

            if type(ForceLoadSession) == "table" then
                local v128;

                if ForceLoadSession[1] == PlaceId then
                    v128 = ForceLoadSession[2] == JobId;
                else
                    v128 = false;
                end;

                v127 = not v128;
            else
                v127 = false;
            end;

            local v129 = u113:IsActive();

            if v127 == true and v126 == true then
                if v129 == true then
                    SaveProfileAsync(u113, true, false, "External");
                end;

                break;
            end;

            local locked_global_updates = u113.locked_global_updates;
            local received_global_updates = u113.received_global_updates;
            local v130 = {};
            local v131 = {};

            for _, v in ipairs(v124.GlobalUpdates[2]) do
                if locked_global_updates[v[1]] == true then
                    v131[v[1]] = true;
                elseif received_global_updates[v[1]] ~= true then
                    received_global_updates[v[1]] = true;
                    table.insert(v130, v);
                end;
            end;

            for i in pairs(locked_global_updates) do
                if v131[i] ~= true then
                    locked_global_updates[i] = nil;
                end;
            end;

            u113.KeyInfo = v125;
            u113.LastSavedData = v124.Data;
            u113.global_updates = v124.GlobalUpdates and v124.GlobalUpdates[2] or {};

            if v126 == true then
                if v129 == true and u114 ~= true then
                    for _, v in ipairs(v130) do
                        local u132 = v[1];
                        local v133 = v[#v];

                        for _, v2 in ipairs(u113.message_handlers) do
                            local u134 = false;

                            local function v135() -- Line: 840
                                -- upvalues: u134 (ref), locked_global_updates (copy), u132 (copy)
                                u134 = true;
                                locked_global_updates[u132] = true;
                            end;

                            local v136 = DeepCopyTable(v133);
                            task.spawn(v2, v136, v135);

                            if u134 == true then
                                break;
                            end;
                        end;
                    end;
                end;
            else
                if u113.roblox_message_subscription ~= nil then
                    u113.roblox_message_subscription:Disconnect();
                end;

                if v129 == true then
                    u27[u113.session_token] = nil;
                    local v137 = table.find(u28, u113);

                    if v137 ~= nil then
                        table.remove(u28, v137);

                        if v137 < u30 then
                            u30 = u30 - 1;
                        end;

                        if u28[u30] == nil then
                            u30 = 1;
                        end;
                    end;

                    u113.OnSessionEnd:Fire();
                end;
            end;

            u113.OnAfterSave:Fire(u113.LastSavedData);
        end;
    end;

    u34 = u34 - 1;
end;

local u138 = {};
u138.__index = u138;

function u138.New(p139, p140, p141, p142, p143, p144) -- Line: 1004
    -- upvalues: DeepCopyTable (copy), u26 (ref), u138 (copy)
    local v145 = p139.Data or {};
    local v146;

    if p139.MetaData then
        v146 = p139.MetaData.ActiveSession or nil;
    else
        v146 = nil;
    end;

    local v147 = p139.GlobalUpdates and (p139.GlobalUpdates[2] or {}) or {};
    local v148 = {};

    for _, v in ipairs(v147) do
        v148[v[1]] = true;
    end;

    local v149 = {
        Data = v145,
        LastSavedData = DeepCopyTable(v145),
        FirstSessionTime = p139.MetaData and (p139.MetaData.ProfileCreateTime or 0) or 0,
        SessionLoadCount = p139.MetaData and (p139.MetaData.SessionLoadCount or 0) or 0,
        Session = v146 and {
            PlaceId = v146[1],
            JobId = v146[2]
        },
        RobloxMetaData = p139.RobloxMetaData or {},
        UserIds = p139.UserIds or {},
        KeyInfo = p140,
        OnAfterSave = u26.New(),
        OnSave = u26.New(),
        OnLastSave = u26.New(),
        OnSessionEnd = u26.New(),
        ProfileStore = p141,
        Key = p142,
        load_timestamp = os.clock(),
        is_mock = p143,
        session_token = p144 or "",
        load_index = p139.MetaData and p139.MetaData.SessionLoadCount or 0,
        locked_global_updates = {},
        received_global_updates = v148,
        message_handlers = {},
        global_updates = v147
    };
    setmetatable(v149, u138);

    return v149;
end;

function u138.IsActive(p150) -- Line: 1050
    -- upvalues: u27 (copy)
    return u27[p150.session_token] == p150;
end;

function u138.Reconcile(p151) -- Line: 1054
    -- upvalues: ReconcileTable (copy)
    ReconcileTable(p151.Data, p151.ProfileStore.template);
end;

function u138.EndSession(p152) -- Line: 1058
    -- upvalues: SaveProfileAsync (copy)
    if p152:IsActive() == true then
        task.spawn(SaveProfileAsync, p152, true, nil, "Manual");
    end;
end;

function u138.AddUserId(p153, p154) -- Line: 1064
    -- upvalues: u37 (ref)
    if type(p154) ~= "number" or p154 % 1 ~= 0 then
        warn(`[{script.Name}]: Invalid UserId argument for :AddUserId() ({tostring(p154)}); Traceback:\n` .. debug.traceback());

        return;
    end;

    if p154 < 0 and (p153.is_mock ~= true and u37 == "Access") then
        return;
    end;

    if table.find(p153.UserIds, p154) == nil then
        table.insert(p153.UserIds, p154);
    end;
end;

function u138.RemoveUserId(p155, p156) -- Line: 1082
    if type(p156) ~= "number" or p156 % 1 ~= 0 then
        warn(`[{script.Name}]: Invalid UserId argument for :RemoveUserId() ({tostring(p156)}); Traceback:\n` .. debug.traceback());

        return;
    end;

    local v157 = table.find(p155.UserIds, p156);

    if v157 ~= nil then
        table.remove(p155.UserIds, v157);
    end;
end;

function u138.SetAsync(p158) -- Line: 1098
    -- upvalues: SaveProfileAsync (copy)
    if p158.view_mode ~= true then
        error((`[{script.Name}]: :SetAsync() can only be used in view mode`));
    end;

    SaveProfileAsync(p158, nil, true);
end;

function u138.MessageHandler(p159, p160) -- Line: 1106
    -- upvalues: DeepCopyTable (copy)
    if type(p160) ~= "function" then
        error((`[{script.Name}]: fn argument is not a function`));
    end;

    if p159.view_mode ~= true and p159:IsActive() ~= true then
        return;
    end;

    local locked_global_updates = p159.locked_global_updates;
    table.insert(p159.message_handlers, p160);

    for _, v in ipairs(p159.global_updates) do
        local u161 = v[1];
        local v162 = v[#v];

        if locked_global_updates[u161] ~= true then
            local v163 = DeepCopyTable(v162);
            task.spawn(p160, v163, function() -- Line: 1123
                -- upvalues: locked_global_updates (copy), u161 (copy)
                locked_global_updates[u161] = true;
            end);
        end;
    end;
end;

function u138.Save(p164) -- Line: 1134
    -- upvalues: u27 (copy), u28 (copy), u30 (ref), u31 (ref), SaveProfileAsync (copy)
    if p164.view_mode == true then
        error((`[{script.Name}]: Can't save profile in view mode; Should you be calling :SetAsync() instead?`));
    end;

    if p164:IsActive() == false then
        warn(`[{script.Name}]: Attempted saving an inactive profile (STORE:{p164.ProfileStore.Name}; KEY:{p164.Key});` .. " Traceback:\n" .. debug.traceback());

        return;
    end;

    u27[p164.session_token] = nil;
    local v165 = table.find(u28, p164);

    if v165 ~= nil then
        table.remove(u28, v165);

        if v165 < u30 then
            u30 = u30 - 1;
        end;

        if u28[u30] == nil then
            u30 = 1;
        end;
    end;

    u27[p164.session_token] = p164;
    table.insert(u28, u30, p164);

    if #u28 > 1 then
        u30 = u30 + 1;
    elseif #u28 == 1 then
        u31 = os.clock();
    end;

    task.spawn(SaveProfileAsync, p164);
end;

local u166 = {
    IsClosing = false,
    IsCriticalState = false,
    DataStoreState = "NotReady",
    OnError = u41,
    OnOverwrite = u42,
    OnCriticalToggle = u26.New()
};
u166.__index = u166;

function u166.SetConstant(p167, p168) -- Line: 1167
    -- upvalues: u1 (ref), u2 (ref), u3 (ref), u4 (ref), u5 (ref), u6 (ref), u7 (ref), u8 (ref), u9 (ref), u10 (ref)
    if type(p168) ~= "number" then
        error((`[{script.Name}]: Invalid value type`));
    end;

    if p167 == "AUTO_SAVE_PERIOD" then
        u1 = p168;

        return;
    end;

    if p167 == "LOAD_REPEAT_PERIOD" then
        u2 = p168;

        return;
    end;

    if p167 == "FIRST_LOAD_REPEAT" then
        u3 = p168;

        return;
    end;

    if p167 == "SESSION_STEAL" then
        u4 = p168;

        return;
    end;

    if p167 == "ASSUME_DEAD" then
        u5 = p168;

        return;
    end;

    if p167 == "START_SESSION_TIMEOUT" then
        u6 = p168;

        return;
    end;

    if p167 == "CRITICAL_STATE_ERROR_COUNT" then
        u7 = p168;

        return;
    end;

    if p167 == "CRITICAL_STATE_ERROR_EXPIRE" then
        u8 = p168;

        return;
    end;

    if p167 == "CRITICAL_STATE_EXPIRE" then
        u9 = p168;

        return;
    end;

    if p167 == "MAX_MESSAGE_QUEUE" then
        u10 = p168;

        return;
    end;

    error((`[{script.Name}]: Invalid constant name was provided`));
end;

function u166.Test() -- Line: 1197
    -- upvalues: u27 (copy), u28 (copy), u33 (ref), u34 (ref), u38 (copy), u39 (copy), u43 (copy)
    return {
        ActiveSessionCheck = u27,
        AutoSaveList = u28,
        ActiveProfileLoadJobs = u33,
        ActiveProfileSaveJobs = u34,
        MockStore = u38,
        UserMockStore = u39,
        UpdateQueue = u43
    };
end;

function u166.New(u169, p170) -- Line: 1209
    -- upvalues: u40 (ref), u166 (copy), u37 (ref), DataStoreService (copy)
    local v171 = p170 or {};

    if type(u169) == "string" then
        if string.len(u169) == 0 then
            error((`[{script.Name}]: store_name cannot be an empty string`));
        elseif string.len(u169) > 50 then
            error((`[{script.Name}]: store_name is too long`));
        end;
    else
        error((`[{script.Name}]: Invalid or missing "store_name"`));
    end;

    if type(v171) ~= "table" then
        error((`[{script.Name}]: Invalid template argument`));
    end;

    local u172 = nil;
    u172 = {
        data_store = nil,
        is_ready = true,
        Mock = {
            Name = u169,

            StartSessionAsync = function(p173, p174) -- Line: 1231, Name: StartSessionAsync
                -- upvalues: u40 (ref), u172 (ref)
                u40 = true;

                return u172:StartSessionAsync(p174);
            end,

            MessageAsync = function(p175, p176, p177) -- Line: 1235, Name: MessageAsync
                -- upvalues: u40 (ref), u172 (ref)
                u40 = true;

                return u172:MessageAsync(p176, p177);
            end,

            GetAsync = function(p178, p179, p180) -- Line: 1239, Name: GetAsync
                -- upvalues: u40 (ref), u172 (ref)
                u40 = true;

                return u172:GetAsync(p179, p180);
            end,

            VersionQuery = function(p181, p182, p183, p184, p185) -- Line: 1243, Name: VersionQuery
                -- upvalues: u40 (ref), u172 (ref)
                u40 = true;

                return u172:VersionQuery(p182, p183, p184, p185);
            end,

            RemoveAsync = function(p186, p187) -- Line: 1247, Name: RemoveAsync
                -- upvalues: u40 (ref), u172 (ref)
                u40 = true;

                return u172:RemoveAsync(p187);
            end
        },
        Name = u169,
        template = v171,
        load_jobs = {},
        mock_load_jobs = {}
    };
    setmetatable(u172, u166);
    local DataStoreOptions = Instance.new("DataStoreOptions");
    DataStoreOptions:SetExperimentalFeatures({
        v2 = true
    });

    if u37 == "NotReady" then
        u172.is_ready = false;
        task.spawn(function() -- Line: 1271
            -- upvalues: u37 (ref), u172 (ref), DataStoreService (ref), u169 (copy), DataStoreOptions (copy)
            repeat
                task.wait();
            until u37 ~= "NotReady";

            if u37 == "Access" then
                u172.data_store = DataStoreService:GetDataStore(u169, nil, DataStoreOptions);
            end;

            u172.is_ready = true;
        end);
    elseif u37 == "Access" then
        u172.data_store = DataStoreService:GetDataStore(u169, nil, DataStoreOptions);
    end;

    return u172;
end;

function u166.StartSessionAsync(u188, p189, p190) -- Line: 1289
    -- upvalues: u40 (ref), u166 (copy), u37 (ref), u27 (copy), u33 (ref), u32 (ref), HttpService (copy), UpdateAsync (copy), PlaceId (copy), JobId (copy), u5 (ref), DeepCopyTable (copy), u138 (copy), u28 (copy), u30 (ref), u31 (ref), MessagingService (copy), SaveProfileAsync (copy), u4 (ref), u2 (ref), u3 (ref), u6 (ref)
    local v191 = u40;
    u40 = false;

    if type(p189) == "string" then
        if string.len(p189) == 0 then
            error((`[{script.Name}]: Invalid profile_key`));
        elseif string.len(p189) > 50 then
            error((`[{script.Name}]: profile_key is too long`));
        end;
    else
        error((`[{script.Name}]: profile_key must be a string`));
    end;

    if p190 ~= nil and type(p190) ~= "table" then
        error((`[{script.Name}]: Invalid params`));
    end;

    if u166.IsClosing == true then
        return nil;
    end;

    local u192 = p190 or {};

    while u188.is_ready == false do
        task.wait();
    end;

    local v193 = (v191 == true and "U_" or (u37 ~= "Access" and "M_" or "L_")) .. u188.Name .. "\0" .. p189;

    if u27[v193] ~= nil then
        error((`[{script.Name}]: Profile (STORE:{u188.Name}; KEY:{p189}) is already loaded in this session`));
    end;

    u33 = u33 + 1;
    local u194 = false;

    local function cancel_condition() -- Line: 1322
        -- upvalues: u194 (ref), u192 (ref)
        if u194 ~= false then
            return true;
        end;

        if u192.Cancel ~= nil then
            u194 = u192.Cancel() == true;
        end;

        return u194;
    end;

    local u195 = u192.Steal == true;
    local v196 = os.clock();
    local v197 = 0;
    local u198 = true;
    local v199 = 1;
    local u200 = false;

    while true do
        while true do
            if u166.IsClosing ~= false then
                u33 = u33 - 1;

                return nil;
            end;

            local v201;

            if u194 == false then
                if u192.Cancel ~= nil then
                    if u192.Cancel() == true then
                        u194 = true;
                    else
                        u194 = false;
                    end;
                end;

                v201 = u194;
            else
                v201 = true;
            end;

            if v201 ~= false then
                u33 = u33 - 1;

                return nil;
            end;

            u32 = u32 + 1;
            local v202 = u32;
            local v203 = v191 == true and u188.mock_load_jobs or u188.load_jobs;
            local v204 = v203[p189];
            local u205 = HttpService:GenerateGUID(false);
            local v206, v207;

            if v204 == nil then
                local v208 = { v202, nil };
                v203[p189] = v208;
                v208[2] = table.pack(UpdateAsync(u188, p189, {
                    ExistingProfileHandle = function(p209) -- Line: 1373, Name: ExistingProfileHandle
                        -- upvalues: u166 (ref), u194 (ref), u192 (ref), PlaceId (ref), JobId (ref), u205 (copy), u5 (ref), u200 (ref), u195 (copy), u198 (ref)
                        if u166.IsClosing ~= true then
                            local v210;

                            if u194 == false then
                                if u192.Cancel ~= nil then
                                    u194 = u192.Cancel() == true;
                                end;

                                v210 = u194;
                            else
                                v210 = true;
                            end;

                            if v210 ~= true then
                                local ActiveSession = p209.MetaData.ActiveSession;
                                local ForceLoadSession = p209.MetaData.ForceLoadSession;

                                if ActiveSession ~= nil then
                                    if type(ActiveSession) == "table" then
                                        local v211;

                                        if ActiveSession[1] == PlaceId then
                                            v211 = ActiveSession[2] == JobId;
                                        else
                                            v211 = false;
                                        end;

                                        if v211 == false then
                                            local LastUpdate = p209.MetaData.LastUpdate;

                                            if LastUpdate ~= nil and u5 < os.time() - LastUpdate then
                                                p209.MetaData.ActiveSession = { PlaceId, JobId, u205 };
                                                p209.MetaData.ForceLoadSession = nil;

                                                return;
                                            end;

                                            if u200 == true or u195 == true then
                                                local v212;

                                                if ForceLoadSession == nil then
                                                    v212 = true;
                                                else
                                                    local v213;

                                                    if ForceLoadSession[1] == PlaceId then
                                                        v213 = ForceLoadSession[2] == JobId;
                                                    else
                                                        v213 = false;
                                                    end;

                                                    v212 = not v213;
                                                end;

                                                if v212 == false or u195 == true then
                                                    p209.MetaData.ActiveSession = { PlaceId, JobId, u205 };
                                                    p209.MetaData.ForceLoadSession = nil;

                                                    return;
                                                end;
                                            elseif u198 == true then
                                                p209.MetaData.ForceLoadSession = { PlaceId, JobId };

                                                return;
                                            end;
                                        else
                                            p209.MetaData.ForceLoadSession = nil;
                                        end;
                                    end;

                                    return;
                                end;

                                p209.MetaData.ActiveSession = { PlaceId, JobId, u205 };
                                p209.MetaData.ForceLoadSession = nil;
                            end;
                        end;
                    end,

                    MissingProfileHandle = function(p214) -- Line: 1410, Name: MissingProfileHandle
                        -- upvalues: u166 (ref), u194 (ref), u192 (ref), DeepCopyTable (ref), u188 (copy), PlaceId (ref), JobId (ref), u205 (copy)
                        local v215;

                        if u166.IsClosing == true then
                            v215 = true;
                        else
                            local v216;

                            if u194 == false then
                                if u192.Cancel ~= nil then
                                    u194 = u192.Cancel() == true;
                                end;

                                v216 = u194;
                            else
                                v216 = true;
                            end;

                            v215 = v216 == true;
                        end;

                        p214.Data = DeepCopyTable(u188.template);
                        p214.MetaData = {
                            SessionLoadCount = 0,
                            ForceLoadSession = nil,
                            ProfileCreateTime = os.time(),
                            ActiveSession = v215 == false and { PlaceId, JobId, u205 } or nil,
                            MetaTags = {}
                        };
                    end,

                    EditProfile = function(p217) -- Line: 1422, Name: EditProfile
                        -- upvalues: u166 (ref), u194 (ref), u192 (ref), PlaceId (ref), JobId (ref)
                        if u166.IsClosing ~= true then
                            local v218;

                            if u194 == false then
                                if u192.Cancel ~= nil then
                                    u194 = u192.Cancel() == true;
                                end;

                                v218 = u194;
                            else
                                v218 = true;
                            end;

                            if v218 ~= true then
                                local ActiveSession = p217.MetaData.ActiveSession;

                                if ActiveSession ~= nil then
                                    local v219;

                                    if ActiveSession[1] == PlaceId then
                                        v219 = ActiveSession[2] == JobId;
                                    else
                                        v219 = false;
                                    end;

                                    if v219 == true then
                                        p217.MetaData.SessionLoadCount = p217.MetaData.SessionLoadCount + 1;
                                        p217.MetaData.LastUpdate = os.time();
                                    end;
                                end;
                            end;
                        end;
                    end
                }, v191));

                if v208[1] ~= v202 then
                    u33 = u33 - 1;

                    return nil;
                end;

                v206, v207 = table.unpack(v208[2]);
                v203[p189] = nil;
            else
                v204[1] = v202;

                while v204[2] == nil do
                    task.wait();
                end;

                if v204[1] ~= v202 then
                    u33 = u33 - 1;

                    return nil;
                end;

                v206, v207 = table.unpack(v204[2]);
                v203[p189] = nil;
            end;

            if v206 == nil or v207 == nil then
                break;
            end;

            local ActiveSession = v206.MetaData.ActiveSession;

            if type(ActiveSession) ~= "table" then
                u33 = u33 - 1;

                return nil;
            end;

            local v220;

            if ActiveSession[1] == PlaceId then
                v220 = ActiveSession[2] == JobId;
            else
                v220 = false;
            end;

            if v220 == true then
                local u221 = u138.New(v206, v207, u188, p189, v191, v193);
                local v222 = u221;
                u27[v222.session_token] = v222;
                table.insert(u28, u30, v222);

                if #u28 > 1 then
                    u30 = u30 + 1;
                elseif #u28 == 1 then
                    u31 = os.clock();
                end;

                if v191 ~= true and u37 == "Access" then
                    local u223 = 0;
                    u221.roblox_message_subscription = MessagingService:SubscribeAsync("PS_" .. u205, function(p224) -- Line: 1461
                        -- upvalues: u221 (ref), u223 (ref), SaveProfileAsync (ref)
                        if type(p224.Data) == "table" and (p224.Data.LoadCount == u221.SessionLoadCount and os.clock() - u223 > 6) then
                            u223 = os.clock();

                            if u221:IsActive() == true then
                                if p224.Data.EndSession == true then
                                    SaveProfileAsync(u221, true, false, "External");

                                    return;
                                end;

                                u221:Save();
                            end;
                        end;
                    end);
                end;

                if u166.IsClosing == true then
                    SaveProfileAsync(u221, true);
                    u221 = nil;
                else
                    local v225;

                    if u194 == false then
                        if u192.Cancel ~= nil then
                            if u192.Cancel() == true then
                                u194 = true;
                            else
                                u194 = false;
                            end;
                        end;

                        v225 = u194;
                    else
                        v225 = true;
                    end;

                    if v225 == true then
                        SaveProfileAsync(u221, true);
                        u221 = nil;
                    end;
                end;

                u33 = u33 - 1;

                return u221;
            end;

            if u166.IsClosing == true then
                u33 = u33 - 1;

                return nil;
            end;

            local v226;

            if u194 == false then
                if u192.Cancel ~= nil then
                    if u192.Cancel() == true then
                        u194 = true;
                    else
                        u194 = false;
                    end;
                end;

                v226 = u194;
            else
                v226 = true;
            end;

            if v226 == true then
                u33 = u33 - 1;

                return nil;
            end;

            local ForceLoadSession = v206.MetaData.ForceLoadSession;
            local v227;

            if ForceLoadSession == nil then
                v227 = true;
            else
                local v228;

                if ForceLoadSession[1] == PlaceId then
                    v228 = ForceLoadSession[2] == JobId;
                else
                    v228 = false;
                end;

                v227 = not v228;
            end;

            if v227 ~= false then
                u33 = u33 - 1;

                return nil;
            end;

            if u198 == false then
                v197 = v197 + 1;

                if math.ceil(u4 / u2) <= v197 then
                    u200 = true;
                end;
            end;

            if type(ActiveSession[3]) == "string" then
                MessagingService:PublishAsync("PS_" .. ActiveSession[3], {
                    EndSession = true,
                    LoadCount = v206.MetaData.SessionLoadCount or 0
                });
            end;

            local v229 = os.clock();
            local v230;

            if u198 == true then
                v230 = u3;
            else
                v230 = u2;
            end;

            repeat
                task.wait();
            until v229 + v230 <= os.clock() or u166.IsClosing == true;

            u198 = false;
        end;

        local v231;

        if u192.Cancel == nil then
            v231 = u6 <= os.clock() - v196;
        else
            v231 = false;
        end;

        if v231 == true or u166.IsClosing == true then
            break;
        end;

        local v232;

        if u194 == false then
            if u192.Cancel ~= nil then
                if u192.Cancel() == true then
                    u194 = true;
                else
                    u194 = false;
                end;
            end;

            v232 = u194;
        else
            v232 = true;
        end;

        if v232 == true then
            break;
        end;

        task.wait(v199);
        v199 = math.min(20, v199 * 2);
    end;

    u33 = u33 - 1;

    return nil;
end;

function u166.MessageAsync(p233, p234, u235) -- Line: 1561
    -- upvalues: u40 (ref), u166 (copy), UpdateAsync (copy), u10 (ref), u37 (ref), u27 (copy), MessagingService (copy)
    local v236 = u40;
    u40 = false;

    if type(p234) == "string" then
        if string.len(p234) == 0 then
            error((`[{script.Name}]: Invalid profile_key`));
        elseif string.len(p234) > 50 then
            error((`[{script.Name}]: profile_key is too long`));
        end;
    else
        error((`[{script.Name}]: profile_key must be a string`));
    end;

    if type(u235) ~= "table" then
        error((`[{script.Name}]: message must be a table`));
    end;

    if u166.IsClosing == true then
        return false;
    end;

    while p233.is_ready == false do
        task.wait();
    end;

    local v237 = 1;

    while u166.IsClosing == false do
        local v240 = UpdateAsync(p233, p234, {
            ExistingProfileHandle = nil,
            MissingProfileHandle = nil,

            EditProfile = function(p238) -- Line: 1590, Name: EditProfile
                -- upvalues: u235 (copy), u10 (ref)
                local GlobalUpdates = p238.GlobalUpdates;
                local v239 = GlobalUpdates[2];
                GlobalUpdates[1] = GlobalUpdates[1] + 1;
                table.insert(v239, { GlobalUpdates[1], u235 });

                while u10 < #v239 do
                    table.remove(v239, 1);
                end;
            end
        }, v236);

        if v240 ~= nil then
            local v241 = u27[(v236 == true and "U_" or (u37 ~= "Access" and "M_" or "L_")) .. p233.Name .. "\0" .. p234];

            if v241 == nil then
                local v242 = v240.MetaData or {};
                local ActiveSession = v242.ActiveSession;
                local v243 = v242.SessionLoadCount or 0;

                if type(ActiveSession) == "table" and type(ActiveSession[3]) == "string" then
                    MessagingService:PublishAsync("PS_" .. ActiveSession[3], {
                        LoadCount = v243
                    });
                end;
            else
                v241:Save();
            end;

            return true;
        end;

        task.wait(v237);
        v237 = math.min(20, v237 * 2);
    end;

    return false;
end;

function u166.GetAsync(u244, p245, p246) -- Line: 1640
    -- upvalues: u40 (ref), u166 (copy), u37 (ref), UpdateAsync (copy), DeepCopyTable (copy), u138 (copy)
    local v247 = u40;
    u40 = false;

    if type(p245) == "string" then
        if string.len(p245) == 0 then
            error((`[{script.Name}]: Invalid profile_key`));
        elseif string.len(p245) > 50 then
            error((`[{script.Name}]: profile_key is too long`));
        end;
    else
        error((`[{script.Name}]: profile_key must be a string`));
    end;

    if u166.IsClosing == true then
        return nil;
    end;

    while u244.is_ready == false do
        task.wait();
    end;

    if p246 ~= nil and (v247 or u37 ~= "Access") then
        return nil;
    end;

    local v248 = 1;

    while u166.IsClosing == false do
        local v250, v251 = UpdateAsync(u244, p245, {
            ExistingProfileHandle = nil,
            EditProfile = nil,

            MissingProfileHandle = function(p249) -- Line: 1671, Name: MissingProfileHandle
                -- upvalues: DeepCopyTable (ref), u244 (copy)
                p249.Data = DeepCopyTable(u244.template);
                p249.MetaData = {
                    SessionLoadCount = 0,
                    ActiveSession = nil,
                    ForceLoadSession = nil,
                    ProfileCreateTime = os.time(),
                    MetaTags = {}
                };
            end
        }, v247, true, p246);

        if v250 ~= nil then
            if v251 == nil then
                return nil;
            end;

            local v252 = u138.New(v250, v251, u244, p245, v247);
            v252.view_mode = true;

            return v252;
        end;

        task.wait(v248);
        v248 = math.min(20, v248 * 2);
    end;

    return nil;
end;

function u166.RemoveAsync(u253, u254) -- Line: 1708
    -- upvalues: u40 (ref), u166 (copy), WaitInUpdateQueue (copy), u37 (ref), u39 (copy), u38 (copy)
    local v255 = u40;
    u40 = false;

    if type(u254) ~= "string" or string.len(u254) == 0 then
        error((`[{script.Name}]: Invalid profile_key`));
    end;

    if u166.IsClosing == true then
        return false;
    end;

    while u253.is_ready == false do
        task.wait();
    end;

    local v256 = WaitInUpdateQueue((v255 == true and "U_" or (u37 ~= "Access" and "M_" or "L_")) .. u253.Name .. "\0" .. u254);
    local v257;

    if v255 == true then
        local v258 = u39[u253.Name];

        if v258 ~= nil then
            v258[u254] = nil;

            if next(v258) == nil then
                u39[u253.Name] = nil;
            end;
        end;

        task.wait();
        v257 = true;
    elseif u37 == "Access" then
        v257 = pcall(function() -- Line: 1750
            -- upvalues: u253 (copy), u254 (copy)
            u253.data_store:RemoveAsync(u254);
        end);
    else
        local v259 = u38[u253.Name];

        if v259 ~= nil then
            v259[u254] = nil;

            if next(v259) == nil then
                u38[u253.Name] = nil;
            end;
        end;

        task.wait();
        v257 = true;
    end;

    v256();

    return v257;
end;

local u260 = {};
u260.__index = u260;

function u260.New(p261, p262, p263, p264, p265) -- Line: 1763
    -- upvalues: u260 (copy)
    local v266 = {
        query_pages = nil,
        query_index = 0,
        query_failure = false,
        is_query_yielded = false,
        profile_store = p261,
        profile_key = p262,
        sort_direction = p263,
        min_date = p264,
        max_date = p265,
        query_queue = {}
    };
    setmetatable(v266, u260);

    return v266;
end;

function MoveVersionQueryQueue(p267)
    while #p267.query_queue > 0 do
        local v268 = table.remove(p267.query_queue, 1);
        task.spawn(v268);

        if p267.is_query_yielded == true then
            break;
        end;
    end;
end;

local u269 = false;

function u260.NextAsync(u270) -- Line: 1797
    -- upvalues: u269 (ref)
    local v271 = u269 == true;
    u269 = false;

    if u270.profile_store == nil then
        return nil;
    end;

    while u270.profile_store.is_ready == false do
        task.wait();
    end;

    local u272 = nil;
    local u273 = false;

    local function query_job() -- Line: 1810
        -- upvalues: u270 (copy), u273 (ref), u269 (ref), u272 (ref)
        if u270.query_failure == true then
            u273 = true;

            return;
        end;

        if u270.query_pages == nil then
            u270.is_query_yielded = true;
            task.spawn(function() -- Line: 1821
                -- upvalues: u269 (ref), u272 (ref), u270 (ref), u273 (ref)
                u269 = true;
                u272 = u270:NextAsync();
                u273 = true;
            end);
            local success, result = pcall(function() -- Line: 1827
                -- upvalues: u270 (ref)
                u270.query_pages = u270.profile_store.data_store:ListVersionsAsync(u270.profile_key, u270.sort_direction, u270.min_date, u270.max_date);
                u270.query_index = 0;
            end);

            if success == false or u270.query_pages == nil then
                warn((`[{script.Name}]: Version query fail - {tostring(result)}`));
                u270.query_failure = true;
            end;

            u270.is_query_yielded = false;
            MoveVersionQueryQueue(u270);

            return;
        end;

        local v274 = u270.query_pages:GetCurrentPage()[u270.query_index + 1];

        if u270.query_pages.IsFinished == true and v274 == nil then
            u273 = true;

            return;
        end;

        if v274 ~= nil then
            local v275 = u270;
            v275.query_index = v275.query_index + 1;
            u272 = u270.profile_store:GetAsync(u270.profile_key, v274.Version);
            u273 = true;

            return;
        end;

        u270.is_query_yielded = true;
        task.spawn(function() -- Line: 1863
            -- upvalues: u269 (ref), u272 (ref), u270 (ref), u273 (ref)
            u269 = true;
            u272 = u270:NextAsync();
            u273 = true;
        end);
        local success, _ = pcall(function() -- Line: 1869
            -- upvalues: u270 (ref)
            u270.query_pages:AdvanceToNextPageAsync();
            u270.query_index = 0;
        end);

        if success == false or #u270.query_pages:GetCurrentPage() == 0 then
            u270.query_failure = true;
        end;

        u270.is_query_yielded = false;
        MoveVersionQueryQueue(u270);
    end;

    if u270.is_query_yielded == false then
        query_job();
    elseif v271 == true then
        table.insert(u270.query_queue, 1, query_job);
    else
        table.insert(u270.query_queue, query_job);
    end;

    while u273 == false do
        task.wait();
    end;

    return u272;
end;

local u276 = false;

function u166.VersionQuery(p277, p278, p279, p280, p281) -- Line: 1910
    -- upvalues: u40 (ref), u166 (copy), u260 (copy), u37 (ref), u36 (copy), u276 (ref)
    local v282 = u40;
    u40 = false;

    if type(p278) ~= "string" or string.len(p278) == 0 then
        error((`[{script.Name}]: Invalid profile_key`));
    end;

    if u166.IsClosing == true then
        return setmetatable({}, u260);
    end;

    if v282 or u37 ~= "Access" then
        if u36 == true and u276 == false then
            u276 = true;
            warn((`[{script.Name}]: :VersionQuery() is not supported in mock mode!`));
        end;

        return setmetatable({}, u260);
    end;

    if p279 ~= nil and (typeof(p279) ~= "EnumItem" or p279.EnumType ~= Enum.SortDirection) then
        error((`[{script.Name}]: Invalid sort_direction ({tostring(p279)})`));
    end;

    if p280 ~= nil and (typeof(p280) ~= "DateTime" and typeof(p280) ~= "number") then
        error((`[{script.Name}]: Invalid min_date ({tostring(p280)})`));
    end;

    if p281 ~= nil and (typeof(p281) ~= "DateTime" and typeof(p281) ~= "number") then
        error((`[{script.Name}]: Invalid max_date ({tostring(p281)})`));
    end;

    if typeof(p280) == "DateTime" then
        p280 = p280.UnixTimestampMillis or p280;
    end;

    if typeof(p281) == "DateTime" then
        p281 = p281.UnixTimestampMillis or p281;
    end;

    return u260.New(p277, p278, p279, p280, p281);
end;

if u36 == true then
    task.spawn(function() -- Line: 1955
        -- upvalues: DataStoreService (copy), u37 (ref), u166 (copy)
        local success, result = pcall(function() -- Line: 1958
            -- upvalues: DataStoreService (ref)
            DataStoreService:GetDataStore("____PS"):SetAsync("____PS", os.time());
        end);
        local v283;

        if success == false then
            v283 = string.find(result, "ConnectFail", 1, true) ~= nil;
        else
            v283 = false;
        end;

        if v283 == true then
            warn((`[{script.Name}]: No internet access - check your network connection`));
        end;

        local v284;

        if success == false and (string.find(result, "403", 1, true) ~= nil or (string.find(result, "must publish", 1, true) ~= nil or v283 == true)) then
            v284 = v283 == true and "NoInternet" or "NoAccess";
            print((`[{script.Name}]: Roblox API services unavailable - data will not be saved`));
        else
            print((`[{script.Name}]: Roblox API services available - data will be saved`));
            v284 = "Access";
        end;

        u37 = v284;
        u166.DataStoreState = v284;
    end);
else
    u37 = "Access";
    u166.DataStoreState = "Access";
end;

RunService.Heartbeat:Connect(function() -- Line: 1994
    -- upvalues: u28 (copy), u1 (ref), u31 (ref), u30 (ref), SaveProfileAsync (copy), u166 (copy), u29 (copy), u7 (ref), u35 (ref), u9 (ref), u8 (ref)
    local v285 = #u28;

    if v285 > 0 then
        local v286 = u1 / v285;
        local v287 = os.clock();

        while v286 < v287 - u31 do
            u31 = u31 + v286;
            local v288 = u28[u30];

            if v287 - v288.load_timestamp < u1 / 2 then
                v288 = nil;

                for _ = 1, v285 - 1 do
                    u30 = u30 + 1;

                    if v285 < u30 then
                        u30 = 1;
                    end;

                    v288 = u28[u30];

                    if v287 - v288.load_timestamp >= u1 / 2 then
                        break;
                    end;

                    v288 = nil;
                end;
            end;

            u30 = u30 + 1;

            if v285 < u30 then
                u30 = 1;
            end;

            if v288 ~= nil then
                task.spawn(SaveProfileAsync, v288);
            end;
        end;
    end;

    if u166.IsCriticalState == false then
        if u7 <= #u29 then
            u166.IsCriticalState = true;
            u166.OnCriticalToggle:Fire(true);
            u35 = os.clock();
            warn((`[{script.Name}]: Entered critical state`));
        end;
    elseif u7 <= #u29 then
        u35 = os.clock();
    elseif u9 < os.clock() - u35 then
        u166.IsCriticalState = false;
        u166.OnCriticalToggle:Fire(false);
        warn((`[{script.Name}]: Critical state ended`));
    end;

    while true do
        local v289 = u29[1];

        if v289 == nil then
            break;
        end;

        if u8 >= os.clock() - v289 then
            return;
        end;

        table.remove(u29, 1);
    end;
end);
task.spawn(function() -- Line: 2068
    -- upvalues: u37 (ref), u166 (copy), u28 (copy), SaveProfileAsync (copy), u33 (ref), u34 (ref)
    while u37 == "NotReady" do
        task.wait();
    end;

    if u37 == "Access" then
        game:BindToClose(function() -- Line: 2082
            -- upvalues: u166 (ref), u28 (ref), SaveProfileAsync (ref), u33 (ref), u34 (ref)
            u166.IsClosing = true;
            local v290 = {};
            local u291 = 0;

            for i, v in ipairs(u28) do
                v290[i] = v;
            end;

            for _, v in ipairs(v290) do
                if v:IsActive() == true then
                    u291 = u291 + 1;
                    task.spawn(function() -- Line: 2098
                        -- upvalues: SaveProfileAsync (ref), v (copy), u291 (ref)
                        SaveProfileAsync(v, true, nil, "Shutdown");
                        u291 = u291 - 1;
                    end);
                end;
            end;

            while u291 > 0 or (u33 > 0 or u34 > 0) do
                task.wait();
            end;
        end);

        return;
    end;

    game:BindToClose(function() -- Line: 2074
        -- upvalues: u166 (ref)
        u166.IsClosing = true;
        task.wait();
    end);
end);

return u166;