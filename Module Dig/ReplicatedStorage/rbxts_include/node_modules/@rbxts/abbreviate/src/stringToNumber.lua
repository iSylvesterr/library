-- Decompiled with Potassium's decompiler.

return {
    stringToNumber = function(p1, p2) -- Line: 1, Name: stringToNumber
        if not p2 or type(p2) ~= "string" then
            error("stringToNumber had invalid parameters.\nP1 - string: string", 2);
        end;

        local u3 = 1;

        for i, v in pairs(p1._suffixTable) do
            p2 = string.gsub(p2, v, function() -- Line: 9
                -- upvalues: u3 (ref), i (copy)
                u3 = u3 * 10 ^ (i * 3);

                return "";
            end);
        end;

        if not tonumber(p2) then
            error("stringToNumber invalid parameter #1: Expected a string which could be converted to a number, got \"" .. p2 .. "\"", 2);
        end;

        return u3 * p2;
    end
};