-- Decompiled with Potassium's decompiler.

return {
    commify = function(p1, p2) -- Line: 1, Name: commify
        local v3 = type(p2) == "number";
        assert(v3, "Attempt to commify a non-number value");
        local v4 = tostring(p2);
        local v5;

        repeat
            v4, v5 = string.gsub(v4, "^(-?%d+)(%d%d%d)", "%1,%2");
        until v5 == 0;

        return v4;
    end
};