-- Decompiled with Potassium's decompiler.

local ReplicatedStorage = game:GetService("ReplicatedStorage");
local Packet = require(ReplicatedStorage.SharedModules.Packet);

return {
    Showcase = Packet("ItemShowcase", Packet.Any)
};