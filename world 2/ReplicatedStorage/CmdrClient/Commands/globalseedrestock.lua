-- Decompiled with Potassium's decompiler.

return {
    Name = "globalseedrestock",
    Description = "GLOBAL: roll a fresh Seed Shop shelf on EVERY live server right now, WITHOUT touching the restock timer -- the countdown keeps running and the next natural 5-minute restock overrides this shelf. Clears everyone\'s per-restock purchase allowance and toasts ADMIN SEED RESTOCK. Use forcerestock instead when you want the clock reset too.",
    Group = "DefaultAdmin",
    Aliases = { "globalseedrestock", "gseedrestock" },
    Args = {}
};