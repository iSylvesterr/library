-- Decompiled with Potassium's decompiler.

local UnixTimestamp = DateTime.fromUniversalTime(2026, 4, 9, 0, 0, 0).UnixTimestamp;

return table.freeze({
    STARTS_AT = UnixTimestamp,
    EXPIRES_AT = UnixTimestamp + 2592000
});