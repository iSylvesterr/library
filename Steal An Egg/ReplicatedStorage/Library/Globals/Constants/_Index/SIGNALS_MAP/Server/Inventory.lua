-- Decompiled with Potassium's decompiler.

return {
    ADD = `{script.Name}: Add`,
    REMOVE = `{script.Name}: Remove`,
    GET = `{script.Name}: Get`,
    MARK_SAFE_ZONE_NOTIFIED = `{script.Name}: MarkSlapSafeZoneNotified`,
    ADD_OFFLINE_POTION_GEAR = `{script.Name}: AddOfflinePotionGear`
};